# Brainstorming session artifacts (visual companion)
.superpowers/

# Git worktrees (isolated phase workspaces)
.worktrees/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Node / future build outputs (Tauri React frontend)
node_modules/
dist/
dist-ssr/
*.local

# Tauri / Rust build outputs (future)
target/
src-tauri/target/
src-tauri/gen/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Excel temp / lock files
~$*.xlsx
~$*.xls

# Third-party copyrighted content that must not be redistributed in this repo.
# /u/happyasianpanda's FIRE flow chart (Reddit-community work) — the Roadmap
# feature is *adapted* from it (attribution in src/legal/disclosures.ts) but
# the PDF + transcription themselves are not ours to ship. Keep them out of
# both the working tree and the public repo.
FinFlowChartv43.pdf
FinFlowChartv43.md

# Trivia generator staging artifact — regenerated on demand via
# GEN_TRIVIA=1 npx vitest run tests/scripts/gen-glossary-trivia.gen.test.ts;
# not committed (human review copy, not the shipped bank).
src/data/trivia/bank-v1.generated.json


# parallel-build git worktrees
.claude/worktrees/
