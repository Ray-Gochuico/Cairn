/** The GitHub releases page — the one URL the app ever opens in the browser
 * (click-only, via the opener plugin). UpdaterSection.tsx carries the same
 * literal today; U4 folds it onto this import. Tauri-free on purpose. */
export const RELEASES_URL = 'https://github.com/Ray-Gochuico/Cairn/releases';
